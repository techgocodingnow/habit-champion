import {
  setSizeMattersBaseHeight,
  setSizeMattersBaseWidth,
} from "@gocodingnow/rn-size-matters";

const SIZE_MATTERS_BASE_WIDTH = 407;
const SIZE_MATTERS_BASE_HEIGHT = 879;
setSizeMattersBaseWidth(SIZE_MATTERS_BASE_WIDTH);
setSizeMattersBaseHeight(SIZE_MATTERS_BASE_HEIGHT);
